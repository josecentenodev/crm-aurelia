/**
 * Página principal del módulo de Pipelines
 * Entry point simple que solo monta el layout principal
 */

import { PipelinesLayout } from "./_layout"

export default function PipelinesPage() {
  return <PipelinesLayout />
}
